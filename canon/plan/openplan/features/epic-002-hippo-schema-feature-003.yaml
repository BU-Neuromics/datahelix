acceptance_criteria:
- Given the schema.yaml file exists, when it is parsed as valid YAML, then a top-level
  entity type named GenomeBuild is present
- Given the GenomeBuild entity type is defined in schema.yaml, when its fields are
  inspected, then a field named genome_assembly exists with type string
- Given the GenomeBuild entity type is defined in schema.yaml, when the genome_assembly
  field attributes are inspected, then it is marked as required
- Given the GenomeBuild entity type is defined in schema.yaml, when its fields are
  inspected, then a field named build_date exists with type date
- Given the GenomeBuild entity type is defined in schema.yaml, when the build_date
  field attributes are inspected, then it is marked as required
- Given the GenomeBuild entity type is defined in schema.yaml, when a YAML parser
  loads the file, then the GenomeBuild definition contains no syntax errors and all
  field type values resolve to valid LinkML types
complexity: low
dependencies: []
description: Implement the GenomeBuild entity type definition in schema.yaml with
  required fields including genome assembly and build date.
id: epic-002-hippo-schema-feature-003
spec_ready: true
title: Define GenomeBuild Entity Type
