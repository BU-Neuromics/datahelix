{
  "name": "bass-certification-scenarios",
  "version": "1.0.0",
  "private": true,
  "description": "Golden-path composition-certification suite — one scenario per product loop (platform ADR-0001)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
