# Hippo configuration — Docker Compose deployment
# See platform/deployment.md for full reference.

storage:
  backend: postgresql
  connection: ${HIPPO_DB}

bridge:
  enabled: true
  trust_proxy:
    - "172.16.0.0/12"      # Docker internal network

schema:
  path: /app/schemas/schema.yaml

log_level: INFO
